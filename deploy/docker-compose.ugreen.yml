services:
  workbase:
    image: workbase:local
    container_name: workbase
    build:
      context: https://github.com/hermansusandy/workbase.git#agent/docker-synology
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: "3000"
      # Replace CHANGE_DATABASE_PASSWORD before deployment. URL-encode special
      # characters such as @, :, /, #, and % in the password.
      DATABASE_URL: "postgresql://Admin:CHANGE_DATABASE_PASSWORD@postgres_db:5432/webnote"
    ports:
      - "3000:3000"
    networks:
      - database_network
    restart: always

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared-tunnel
    # Replace CHANGE_CLOUDFLARE_TUNNEL_TOKEN before deployment.
    command:
      - tunnel
      - --no-autoupdate
      - run
      - --token
      - CHANGE_CLOUDFLARE_TUNNEL_TOKEN
    networks:
      - database_network
    depends_on:
      - workbase
    restart: always

networks:
  database_network:
    external: true
    name: database_network
